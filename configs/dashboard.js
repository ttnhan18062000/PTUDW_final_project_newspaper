const ENDPOINTS = {
  getTotalPost: `Call GTR_Total_Post();`,
  getTotalTag: `Call GTR_Total_Tag();`,
  getTotalCategory: `Call GTR_Total_Category();`,
  getTotalAccount: `Call GTR_Total_Account();`,
  getTotalPremiumRequest: `Call GTR_Total_PremiumRequest();`,
  getTotalView: `Call GTR_Total_View();`,
  getTotalComment: `Call GTR_Total_Comment();`,
  getTotalSubscriber: `Call GTR_Total_Subscriber();`,
  getTotalPremiumSubscriber: `Call GTR_Total_PremiumSubscriber();`,
}
module.exports = {
  ENDPOINTS,
};